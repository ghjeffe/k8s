replicaCount: 2

image:
  repository: pyapp
  tag: "1.0.0"
  pullPolicy: Never  # Image is pre-loaded into kind

service:
  type: ClusterIP
  port: 80
  targetPort: 8080

namespace: new-namespace

istio:
  gateway:
    enabled: true
    hosts:
      - "pyapp.local"
  mtls:
    mode: STRICT
